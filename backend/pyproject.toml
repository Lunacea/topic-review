[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend for topic-review"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.35.0",
]

[tool.uv]
package = false
